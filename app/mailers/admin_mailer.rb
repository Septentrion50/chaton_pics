class AdminMailer < ApplicationMailer
    # def order_email(user, order, charge)
    #     @user = user
    #     @order = order
    #     @charge = charge
    #     mail(to: @user.email, subject: 'Récapitulatif de la commande') 
    # end
end
